// Shared KPI utilities — used by KPIsPage and ScorecardPage.
// The original implementations live in cadenceUtils.ts and are
// re-exported here for a single import point for KPI-specific pages.

export { fmt, isOnTrack, makeTrend } from './cadenceUtils'
